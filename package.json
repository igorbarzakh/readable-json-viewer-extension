{
  "name": "readable-json-viewer-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/viewer-core.test.js",
    "test:e2e": "playwright test",
    "check": "npm run build && npm run test && npm run test:e2e",
    "package": "npm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "bestzip": "^2.2.1",
    "esbuild": "^0.27.3",
    "jsdom": "^28.1.0"
  }
}
