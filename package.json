{
  "name": "readable-json-viewer-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/viewer-core.test.js",
    "build": "node scripts/build.mjs",
    "check": "npm run build && node --test tests/*.test.js",
    "package": "npm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "bestzip": "^2.2.1",
    "esbuild": "^0.27.3",
    "jsdom": "^28.1.0"
  }
}
